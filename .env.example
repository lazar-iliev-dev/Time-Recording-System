# copy this file to .env and set secure values
POSTGRES_USER=timertrakerapp
POSTGRES_PASSWORD=replace_with_strong_password
POSTGRES_DB=timertrakerdb
EDGE_SECRET=supersecret
BACKEND_URL=http://backend:8000
NEXT_PUBLIC_API_URL=http://localhost:3000
EDGE_SECRET=supersecret

# Optional local overrides for dev (you can remove)
# DATABASE_URL can be left empty; compose constructs it for backend
# DATABASE_URL=postgresql://trs_user:replace_with_strong_password@db:5432/trs_db

